1. Написать скрипт, который удаляет из текстового файла пустые строки и заменяет маленькие символы на большие. Воспользуйтесь tr или SED.
===создаем скрипт для выполнения 2-х функций: сначала удаление пустых строк затем капитализации символов===

---
#!/bin/bash
# аргумент 1 это исходный файл 
# аргумент 2 это файл вывода

sed '/^$/d' $1 |    # удаляем пустые строки и передаем по конвееру далее
tr '[:lower:]' '[:upper:]' > $2   # выделяем маленькие буквы и капитализируем их и сохраняем в файл $2
---

===создадим файл для теста скрипта===

----
Hello

up here

HeLoO
-----

===Применяем скрипт====
$ bash taks1.bash in_file.txt out_file.txt

2. Создать однострочный скрипт, который создаст директории для нескольких годов (2010–2017), в них — поддиректории для месяцев (от 01 до 12), и в каждый из них запишет несколько файлов с произвольными записями. Например, 001.txt, содержащий текст «Файл 001», 002.txt с текстом «Файл 002» и т. д.
===Скрипт для создания папок и контента в них===

---
for i in $(seq 2010 2017)
do
  mkdir $i
        for t in $(seq 01 12)
                do
                        mkdir $i/$t
                        echo $i$t >> $i/$t/$t.txt
                done
done
---

# скрипт можно превратить в однострочный поставив ";" везде, где начинается новая строка.
# для целей демонтсрации показываю развернуто
# ох, теперь это еще и удалять

4. Используя grep, проанализировать файл /var/log/syslog, отобрав события на своё усмотрение.
===отбор логов антивируса ClamAV====
$ cat /var/log/syslog | grep ClamAV

5. Создать разовое задание на перезагрузку операционной системы, используя at.
$ sudo reboot | at 11:03pm